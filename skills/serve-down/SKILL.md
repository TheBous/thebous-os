---
name: serve-down
description: Stop and clean up the local service started by serve-up — compose stack, container, or background dev process
---

# Serve down

Read `${CLAUDE_PLUGIN_ROOT}/commands/serve-down.md` and execute every step in it, in order, exactly as written.
