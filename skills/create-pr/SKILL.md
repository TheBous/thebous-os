---
name: create-pr
description: Validate the Jira task, create a PR on GitHub against main, comment on Jira, and notify Slack
---

# Create pr

Read `${CLAUDE_PLUGIN_ROOT}/commands/create-pr.md` and execute every step in it, in order, exactly as written. Do not create the PR until the Jira task validation gate passes.
