---
description: Generate the end-of-day recap (today's work, Claude Code and OpenCode sessions touched today)
---

## Goal

Explicit, guaranteed entry point for the end-of-day skill — invoke this when
the user asks for the recap directly, rather than waiting on description-based
auto-triggering.

## Steps

Read `${CLAUDE_PLUGIN_ROOT}/skills/end-of-day/SKILL.md` and execute every step
in it, in order, exactly as written.
